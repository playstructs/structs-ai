---
layout: null
sitemap: false
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{%- assign collections = site.collections | where_exp: "collection", "collection.output != false" -%}
{%- for collection in collections -%}
  {%- assign docs = collection.docs | where_exp: "doc", "doc.sitemap != false" -%}
  {%- for doc in docs %}
  <url>
    <loc>{{ doc.url | replace: "/index.html", "/" | absolute_url | xml_escape }}</loc>
    {%- if doc.last_modified_at %}
    <lastmod>{{ doc.last_modified_at | date: "%Y-%m-%d" }}</lastmod>
    {%- elsif doc.date %}
    <lastmod>{{ doc.date | date: "%Y-%m-%d" }}</lastmod>
    {%- else %}
    <lastmod>{{ site.time | date: "%Y-%m-%d" }}</lastmod>
    {%- endif %}
  </url>
  {%- endfor -%}
{%- endfor -%}
{%- assign pages = site.html_pages | where_exp: "doc", "doc.sitemap != false" | where_exp: "doc", "doc.url != '/404.html'" -%}
{%- for p in pages -%}
  {%- if p.url == "/sitemap.xml" or p.url == "/sitemap-txt.xml" %}{% continue %}{% endif %}
  <url>
    <loc>{{ p.url | replace: "/index.html", "/" | absolute_url | xml_escape }}</loc>
    {%- if p.last_modified_at %}
    <lastmod>{{ p.last_modified_at | date: "%Y-%m-%d" }}</lastmod>
    {%- elsif p.date %}
    <lastmod>{{ p.date | date: "%Y-%m-%d" }}</lastmod>
    {%- else %}
    <lastmod>{{ site.time | date: "%Y-%m-%d" }}</lastmod>
    {%- endif %}
  </url>
{%- endfor %}
  <url>
    <loc>{{ "/llms.txt" | absolute_url | xml_escape }}</loc>
    <lastmod>{{ site.time | date: "%Y-%m-%d" }}</lastmod>
  </url>
  <url>
    <loc>{{ "/llms-start.txt" | absolute_url | xml_escape }}</loc>
    <lastmod>{{ site.time | date: "%Y-%m-%d" }}</lastmod>
  </url>
</urlset>
